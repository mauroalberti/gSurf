"""
The parts of gSurf that outlive any one tool.

`app` and not `gsurf`: the repository still carries the old `gSurf/` package,
kept for the code worth porting, and on a case-insensitive filesystem -- macOS,
Windows, both of which the GitHub mirror serves -- a `gsurf/` beside it is the
same directory. The collision would not show up here, where it was written.
"""
